# Database Configuration
# Para desarrollo local con Docker: postgres://user:password@localhost:5433/dbname
# Para PostgreSQL local: postgres://user:password@localhost:5432/dbname
DATABASE_URL=postgres://event_saga:event_saga_pass@localhost:5433/event_saga_db?sslmode=disable

# Redpanda/Kafka Configuration
# Múltiples brokers separados por coma: localhost:19092,localhost:19093
KAFKA_BROKERS=localhost:19092

# PostgreSQL Configuration (for docker-compose.yml)
POSTGRES_USER=event_saga
POSTGRES_PASSWORD=event_saga_pass
POSTGRES_DB=event_saga_db
